# 🐍 贪吃蛇小游戏 — 维护记录

**仓库**：https://github.com/maning77/snake-game （分支 main）

> 本文件用于记录每次维护 / 修复的审查结论与修改要点，与仓库提交历史一一对应。

## 提交历史（最新在上）

| 时间 | 提交 | 说明 |
|---|---|---|
| 2026-09-08 11:02 | 第 2 次维护：新增"准备开始"待命态、切走自动暂停、收紧输入边界 | v3.2，详见下方「第 2 次维护」 |
| 2026-09-08 09:50 | 第 1 次维护：修复键盘长按抖动、roundRect 兼容、小屏裁切 | v3.1，详见下方「第 1 次维护」 |
| 2026-09-07 15:30 | v3 体验升级 | 音效 / 难度 / 粒子特效，见提交历史 |
| 2026-09-07 14:35 | v2 增强玩法 | 最高分、暂停、提速、移动端、视觉升级 |
| 2026-09-07 14:33 | v1 基础可玩版本 | 经典玩法实现 |

## 第 1 次维护（2026-09-08）

### 审查范围
通读 `index.html` / `style.css` / `game.js` / `README.md`，针对可复现的缺陷与兼容性做修复，不改变核心玩法。

### 发现并修复的问题

1. **键盘长按连发导致抖动误触**
   - 现象：按住空格 / P / M / 回车时，系统会连续触发 `keydown`（`e.repeat`），造成「暂停 ⇄ 继续」反复切换、静音开关来回跳、游戏结束后按住空格会「刚重开又被暂停」。
   - 修复：`game.js` 键盘监听开头增加 `if (e.repeat) return;`，只响应首次按下。
   - 涉及文件：`game.js`

2. **`ctx.roundRect` 旧浏览器不兼容（潜在白屏）**
   - 现象：`CanvasRenderingContext2D.roundRect` 是较新 API（Chrome 99+ / Safari 16+ / Firefox 112+），旧内核浏览器会直接抛 `TypeError`，游戏画面无法渲染。
   - 修复：`game.js` 新增 `fillRoundRect()` 兼容函数——支持 `roundRect` 时正常调用，否则用手写 `arcTo` 圆角路径绘制；蛇身绘制统一改走该函数。
   - 涉及文件：`game.js`

3. **小屏 / 矮视口下内容被裁切、无法滚动**
   - 现象：页面设了 `overflow: hidden` 且绝对垂直居中，在手机或矮窗口上，HUD + 控制条 + 棋盘 + 方向键总高度超过视口时，上下被裁掉且无法滚动到操作按钮。
   - 修复：`style.css` 改为横向禁滚、纵向 `auto`；垂直居中由「`align-items: center`」改为「`flex-start` + `.game-wrap { margin: auto }`」，空间足够时仍居中，溢出时自动让位可滚动。
   - 涉及文件：`style.css`

### 未改动的说明（供后续维护参考）
- `game.js` 头部注释同步升级为 **v3.1** 并注明本次修改点。
- 本轮为纯修复，未调整玩法参数与难度数值，`index.html` 无改动。

## 第 2 次维护（2026-09-08）

### 审查范围
通读 `game.js` 交互与状态机（键盘 / 触控 / 覆盖层 / 计时器），针对"开局时机与后台行为"做体验与健壮性改进，不改变核心玩法与难度数值。

### 发现并修复的问题

1. **首次打开页面蛇就自动移动（容易"一打开就撞死"）**
   - 现象：页面加载即 `startGame()`，蛇立刻向右走。玩家若没来得及操作，标签页一开就可能撞墙/撞身结束；移动端误触也会导致提前开局。
   - 修复：新增 **"准备开始"(idle) 待命态**——启动时蛇身与食物就位但静止，覆盖层提示「按任意方向键 / WASD / 空格开始」；首次按键/滑动/点按以**该方向**开局（`startGame(h)` / `initSnake(h)` 支持指定初始朝向并把蛇身沿反方向铺设），因此任意方向开局的第一步都不会撞到自己；按钮文案在「开始游戏 ⇄ 重新开始」间自动切换。
   - 涉及文件：`game.js`

2. **切换页面/隐藏标签页后蛇仍在后台继续走，回来常常已撞死**
   - 现象：`requestAnimationFrame` 在后台标签页被节流，恢复前台瞬间可能补跑多步直接撞墙/撞身。
   - 修复：监听 `visibilitychange`，页面隐藏且 `running` 时自动暂停（静音、不播暂停音），回到页面按空格/P 继续即可；恢复/开局统一重置 `lastStep` 计时，杜绝"恢复瞬间补跑多步"。
   - 涉及文件：`game.js`

3. **输入收集时机过宽，暂停/结束期间仍可能攒下危险方向**
   - 现象：旧版只要方向不反向就写入 `nextDir`，暂停或结束时积累的输入会在恢复瞬间生效，容易导致刚恢复就撞墙。
   - 修复：收紧 `setDirection` 边界——仅 `running` 收集方向；`idle` 的首个方向只用于开局、`paused/over` 一律忽略；键盘上空格/P/回车在 idle/over 只负责开局/重开，运行中不干预（`e.repeat` 连发防护保持 v3.1 行为）。
   - 涉及文件：`game.js`

### 说明
- `game.js` 头部注释升级为 **v3.2** 并注明本次修改点；改动后已通过 `node --check` 语法校验。
- `index.html` / `style.css` / `README.md` 本轮无改动。

## 文件说明

- **第 1 次维护（v3.1）**：修改 `game.js`、`style.css`、`README.md`；新增 `维护记录.md`。
- **第 2 次维护（v3.2）**：修改 `game.js`、`维护记录.md`；`index.html` / `style.css` / `README.md` 未变。
