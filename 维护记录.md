# 🐍 贪吃蛇小游戏 — 维护记录

**仓库**：https://github.com/maning77/snake-game （分支 main）

> 本文件用于记录每次维护 / 修复的审查结论与修改要点，与仓库提交历史一一对应。

## 提交历史（最新在上）

| 时间 | 提交 | 说明 |
|---|---|---|
| 2026-09-09 14:30 | 第 6 次维护：C 版加 AI 自动玩 + 逻辑测试 | `snake.c` 加 `ai_pick_direction`，新增 `test_snake.py`，详见下方「第 6 次维护」 |
| 2026-09-08 14:10 | 第 5 次维护：**新增 C 语言控制台版** | `snake.c` + `build.bat` + `run.bat`，详见下方「第 5 次维护」 |
| 2026-09-08 11:30 | 第 4 次维护：整合 Lyw123414 的 v3.1 修复 | 三处修复再次入仓，与第 1 次维护内容一致 |
| 2026-09-08 09:50 | 第 1 次维护：修复键盘长按抖动、roundRect 兼容、小屏裁切 | v3.1，详见下方「第 1 次维护」 |
| 2026-09-07 15:30 | v3 体验升级 | 音效 / 难度 / 粒子特效，见提交历史 |
| 2026-09-07 14:35 | v2 增强玩法 | 最高分、暂停、提速、移动端、视觉升级 |
| 2026-09-07 14:33 | v1 基础可玩版本 | 经典玩法实现 |

## 第 6 次维护（2026-09-09）

### 审查范围
在第 5 次新增的 C 版基础上，给 C 语言控制台版加 **AI 自动玩模式** 与 **不依赖 gcc 的逻辑回归测试**，让 C 版既能自玩也能在没装编译器的环境验证核心算法。

### 改动说明

- **`snake.c`**
  - `Game` 结构新增 `ai_mode` 字段
  - 新增 `ai_pick_direction()` 贪心算法：每步从 4 个方向中选**曼哈顿距离最近**且不撞墙、不撞身、非反向的方向；找不到最优时退回任意安全方向避免卡死；并对"靠墙"位置施加少量安全惩罚，鼓励蛇往中间走
  - `do_game` 在 step 前自动调用 AI 选方向
  - 新增 `K` 键切换 AI 开/关（与方向键 `A` 不冲突）
  - `draw_frame` 状态栏新增 `AI: ON/OFF` 实时显示
  - 菜单提示行同步增加 `K AI自动玩` 说明

- **新增 `test_snake.py`**（约 200 行）
  - 用 Python 复刻 snake.c 的核心数据（蛇身 / 食物 / 方向）与算法（移动 / 食物生成 / 撞墙 / 撞身 / AI 选方向）
  - 9 个断言用例：初始状态、基础移动、撞墙、撞身、吃食物、反向锁定、AI 50 步不死、AI 1000 步有效得分、100 局随机模拟无崩溃
  - **无需 gcc**，纯 Python 跑测试，输出 `9 通过 / 0 失败` 即代表核心算法与 C 版行为一致
  - 为方便差异追踪，关键函数 / 字段命名与 C 版 `snake.c` 一致

- **`README.md`**
  - 操作表中加 `K AI 自动玩切换（仅 C 版）`
  - 文件结构补 `test_snake.py`
  - 「本地运行」章节补「逻辑回归测试」用法

### 测试结果

```
✓ test_initial_state                  初始状态正确
✓ test_step_basic                     基础移动 5 步正确
✓ test_wall_collision                 撞墙判定正确
✓ test_self_collision                 撞自身判定正确
✓ test_eat_food                       吃食物 +10 分与增长正确
✓ test_direction_block                方向锁定正确
✓ test_ai_pick_safe                   AI 50 步未撞死
✓ test_ai_can_win_or_score_well       AI 自动玩得分有效（1000 步得分=20，长度=5）
✓ test_random_games_dont_crash        100 局随机游戏无崩溃
9 通过 / 0 失败
```

### 文件清单

- 修改文件：`snake.c`、`README.md`
- 新增文件：`test_snake.py`

## 第 5 次维护（2026-09-09）

### 审查范围
按用户要求，把整个浏览器版贪吃蛇"全部改成 C 语言"，交付 Windows 控制台可玩的等效版本。

### 改动说明

- **新增 `snake.c`**（约 410 行）
  - 用 `conio.h` 读取按键（`kbhit` / `getch`），用 `windows.h` 控制光标位置 / 文本颜色 / `Beep` 音效
  - 完整保留：20×20 棋盘、方向键 / WASD 控制、禁止 180° 掉头、随机食物（避开蛇身）、吃食物 +10 分与蛇身增长、撞墙 / 撞自身判定、暂停、最高分文件持久化、三档难度（影响初始速度与提速节奏）、吃到食物时粒子爆裂、Beep 音效（吃 / 升级 / 失败 / 暂停）、开始菜单 / 结束覆盖层、一键重开
  - 持久化文件：`snake_best.txt`（最高分）、`snake_diff.txt`（难度偏好）、`snake_muted.txt`（静音偏好）
  - 操作：方向键 / WASD 移动、P 或空格暂停、M 切换静音、ESC 退出、R 或回车重开

- **新增 `build.bat`**
  - 自动探测 `gcc` / `cl` / `tcc` 任一编译器并完成编译
  - 输出 `snake.exe`，编译失败给出提示

- **新增 `run.bat`**
  - 一键编译并运行 C 版（`snake.exe` 不存在时自动调用 `build.bat`）

- **`README.md`**
  - 顶部增加「两个版本任你玩」说明
  - 文件结构补全 C 版相关文件
  - 「本地运行」章节增加 C 语言版的编译与运行说明

### 与浏览器版的差异

| 项目 | 浏览器版 | C 版 |
|---|---|---|
| 渲染 | Canvas 2D | Windows 控制台 ASCII + 颜色 |
| 最高分存储 | `localStorage` | `snake_best.txt` |
| 偏好存储 | `localStorage` | `snake_diff.txt` / `snake_muted.txt` |
| 音效 | Web Audio API | `Beep(freq, ms)` |
| 粒子 | Canvas 动画 | 控制台字符闪烁 |
| 移动端 | 触控按钮 + 滑动手势 | 不支持 |
| 难度 / 静音 / 暂停 | ✅ | ✅ |
| 撞墙 / 撞身判定 | ✅ | ✅ |
| 反向掉头保护 | ✅ | ✅ |

### 文件清单

- 新增文件：`snake.c`、`build.bat`、`run.bat`
- 修改文件：`README.md`
- 本轮未涉及：`index.html`、`style.css`、`game.js`

## 第 1 次维护（2026-09-08）

### 审查范围
通读 `index.html` / `style.css` / `game.js` / `README.md`，针对可复现的缺陷与兼容性做修复，不改变核心玩法。

### 发现并修复的问题

1. **键盘长按连发导致抖动误触**
   - 现象：按住空格 / P / M / 回车时，系统会连续触发 `keydown`（`e.repeat`），造成「暂停 ⇄ 继续」反复切换、静音开关来回跳、游戏结束后按住空格会「刚重开又被暂停」。
   - 修复：`game.js` 键盘监听开头增加 `if (e.repeat) return;`，只响应首次按下。
   - 涉及文件：`game.js`

2. **`ctx.roundRect` 旧浏览器不兼容（潜在白屏）**
   - 现象：`CanvasRenderingContext2D.roundRect` 是较新 API（Chrome 99+ / Safari 16+ / Firefox 112+），旧内核浏览器会直接抛 `TypeError`，游戏画面无法渲染。
   - 修复：`game.js` 新增 `fillRoundRect()` 兼容函数——支持 `roundRect` 时正常调用，否则用手写 `arcTo` 圆角路径绘制；蛇身绘制统一改走该函数。
   - 涉及文件：`game.js`

3. **小屏 / 矮视口下内容被裁切、无法滚动**
   - 现象：页面设了 `overflow: hidden` 且绝对垂直居中，在手机或矮窗口上，HUD + 控制条 + 棋盘 + 方向键总高度超过视口时，上下被裁掉且无法滚动到操作按钮。
   - 修复：`style.css` 改为横向禁滚、纵向 `auto`；垂直居中由「`align-items: center`」改为「`flex-start` + `.game-wrap { margin: auto }`」，空间足够时仍居中，溢出时自动让位可滚动。
   - 涉及文件：`style.css`

### 未改动的说明（供后续维护参考）
- `game.js` 头部注释同步升级为 **v3.1** 并注明本次修改点。
- 本轮为纯修复，未调整玩法参数与难度数值，`index.html` 无改动。

## 文件说明

- 修改文件：`game.js`、`style.css`、`README.md`
- 新增文件：`维护记录.md`（本文件）
- 本轮未涉及：`index.html`
